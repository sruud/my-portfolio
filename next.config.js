/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export", // Export Next.js as a static site
  basePath: "/sruud", // GitHub repo name
  assetPrefix: "/sruud",
};

module.exports = nextConfig;
